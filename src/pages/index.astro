---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('posts', ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const featured = posts[0];
const recent = posts.slice(1, 9);
---

<Layout title="Home">

  {featured && (
    <a href={`/posts/${featured.id}`} class="group block rounded-2xl overflow-hidden bg-surface-2 border border-neutral-200 dark:border-neutral-700 hover:border-[#00a88f] dark:hover:border-[#00a88f] hover:shadow-xl transition-all duration-300 mb-12">
      {featured.data.cover ? (
        <div class="w-full h-64 md:h-96 overflow-hidden">
          <img src={featured.data.cover} alt={featured.data.coverAlt ?? featured.data.title} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" />
        </div>
      ) : (
        <div class="w-full h-48 md:h-72 bg-gradient-to-br from-[#00a88f] to-[#008c72] flex items-center justify-center p-10">
          <span class="text-white text-2xl md:text-4xl font-bold text-center leading-tight">{featured.data.title}</span>
        </div>
      )}
      <div class="p-6">
        <span class="text-xs font-bold uppercase tracking-widest text-[#00a88f]">Featured</span>
        <h2 class="mt-2 text-2xl md:text-3xl font-bold text-neutral-900 dark:text-neutral-100 group-hover:text-[#00a88f] transition-colors leading-tight">
          {featured.data.title}
        </h2>
        <p class="mt-2 text-sm text-neutral-400 dark:text-neutral-500">
          {featured.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
        </p>
        {featured.data.description && (
          <p class="mt-3 text-neutral-600 dark:text-neutral-300 line-clamp-3">{featured.data.description}</p>
        )}
      </div>
    </a>
  )}

  <h2 class="text-xl font-bold text-neutral-900 dark:text-neutral-100 mb-6 pb-3 border-b border-neutral-200 dark:border-neutral-700">Latest Posts</h2>

  {posts.length === 0 ? (
    <p class="text-neutral-500 dark:text-neutral-400">No posts yet. Add markdown files to src/content/posts/</p>
  ) : (
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      {recent.map(post => (
        <a href={`/posts/${post.id}`} class="group flex flex-col rounded-xl overflow-hidden bg-surface-2 border border-neutral-200 dark:border-neutral-700 hover:border-[#00a88f] dark:hover:border-[#00a88f] hover:shadow-md transition-all duration-300">
          {post.data.cover ? (
            <div class="w-full h-44 overflow-hidden">
              <img src={post.data.cover} alt={post.data.coverAlt ?? post.data.title} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" loading="lazy" />
            </div>
          ) : (
            <div class="w-full h-2 bg-gradient-to-r from-[#00a88f] to-[#008c72]"></div>
          )}
          <div class="p-5 flex flex-col flex-1">
            <h3 class="font-bold text-neutral-900 dark:text-neutral-100 group-hover:text-[#00a88f] transition-colors leading-snug">
              {post.data.title}
            </h3>
            <p class="mt-1 text-xs text-neutral-400 dark:text-neutral-500">
              {post.data.date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}
            </p>
            {post.data.description && (
              <p class="mt-2 text-sm text-neutral-600 dark:text-neutral-300 line-clamp-2 flex-1">{post.data.description}</p>
            )}
          </div>
        </a>
      ))}
    </div>
  )}

  <div class="mt-10 text-center">
    <a href="/posts" class="inline-block px-8 py-3 rounded-full bg-[#00a88f] hover:bg-[#008c72] !text-white font-bold text-sm transition-colors">
      View All Posts →
    </a>
  </div>

</Layout>
